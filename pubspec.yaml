name: failure_kit
description: HTTP-client agnostic error handling for Dart/Flutter — Either pattern, typed Failures, pluggable mapper chain, and optional Dio support.
version: 3.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  meta: any
  dio: ^5.9.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
