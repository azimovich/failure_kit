name: failure_kit
description: HTTP-client agnostic error handling for Dart/Flutter — Either pattern, typed Failures, and a pluggable mapper chain.
version: 1.0.0
repository: https://github.com/azimovich/failure_kit

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  meta: any

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  dio: ^5.0.0
