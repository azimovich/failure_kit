name: dart_failure_handler
description: Professional, HTTP-client agnostic error handling package for Dart/Flutter with Either pattern, typed Failures, and optional Dio support.
version: 1.0.2
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=3.0.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  meta: any
  dio: ^5.9.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
